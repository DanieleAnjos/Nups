<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Arquivo</title>
</head>
<body>
  <h1>Editar Arquivo</h1>
  {{#if error_msg}}
    <div class="alert alert-danger">{{error_msg}}</div>
  {{/if}}
  <form action="/arquivos/edit/{{arquivo.id}}" method="POST" enctype="multipart/form-data">
    <div class="form-group">
      <label for="nome">Nome:</label>
      <input type="text" name="nome" id="nome" class="form-control" value="{{arquivo.nome}}" required>
    </div>
    <div class="form-group">
      <label for="descricao">Descrição:</label>
      <textarea name="descricao" id="descricao" class="form-control">{{arquivo.descricao}}</textarea>
    </div>
    <div class="form-group">
      <label for="arquivo">Novo Arquivo (opcional):</label>
      <input type="file" name="arquivo" id="arquivo" class="form-control">
    </div>
    <button type="submit" class="btn btn-primary">Atualizar</button>
  </form>
</body>
</html>