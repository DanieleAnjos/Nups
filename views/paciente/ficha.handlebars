<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ficha do Paciente</title>
  <style>

    .container {
      max-width: 800px;
      padding: 40px;
      background-color: #fff;
      border-radius: 12px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      margin: auto;
    }

    h1, h3, h4 {
      color: #2980b9;
    }

    .section {
      padding: 15px;
      margin-bottom: 20px;
      background: #f9f9f9;
      border-radius: 8px;
    }

    .info-group {
      margin-bottom: 10px;
      font-size: 1rem;
    }

    .info-group strong {
      color: #2c3e50;
    }

    .img-container {
      text-align: center;
      margin-bottom: 20px;
    }

    .img-container img {
      max-width: 200px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .btn-primary {
      display: block;
      padding: 10px;
      font-size: 1rem;
      font-weight: 600;
      color: #fff;
      background-color: #3498db;
      border: none;
      border-radius: 6px;
      text-align: center;
      text-decoration: none;
      transition: background-color 0.3s ease;
      margin-top: 20px;
    }

    .btn-primary:hover {
      background-color: #2980b9;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Ficha do Paciente</h1>

    <div class="img-container">
      {{#if imagePath}}
        <img src="{{imagePath}}" alt="Foto de {{paciente.nome}}">
      {{else}}
        <p>Sem foto disponível</p>
      {{/if}}
    </div>

    <div class="section">
      <h3>Dados Pessoais</h3>
      <p class="info-group"><strong>Nome:</strong> {{paciente.nome}}</p>
      <p class="info-group"><strong>Matrícula:</strong> {{paciente.matricula}}</p>
      <p class="info-group"><strong>Data de Nascimento:</strong> {{Data paciente.dataNascimento}}</p>
      <p class="info-group"><strong>Sexo:</strong> {{paciente.sexo}}</p>
      <p class="info-group"><strong>RG:</strong> {{paciente.rg}}</p>
    </div>

    <div class="section">
      <h3>Contato</h3>
      <p class="info-group"><strong>Telefone:</strong> {{paciente.telefone}}</p>
      <p class="info-group"><strong>Telefone de Contato:</strong> {{paciente.telefoneContato}}</p>
    </div>

    <div class="section">
      <h3>Endereço</h3>
      <p class="info-group"><strong>Endereço:</strong> {{paciente.endereco}}</p>
      <p class="info-group"><strong>CEP:</strong> {{paciente.cep}}</p>
    </div>

    <div class="section">
      <h3>Informações Profissionais</h3>
      <p class="info-group"><strong>Posto de Serviço:</strong> {{paciente.postoServico}}</p>
      <p class="info-group"><strong>Escala:</strong> {{paciente.escala}}</p>
      <p class="info-group"><strong>Tempo de serviço:</strong> {{paciente.periodoEscala}}</p>
    </div>

    <div class="section">
      <h3>Saúde</h3>
      <p class="info-group"><strong>Status:</strong> {{paciente.statusPaciente}}</p>
      <p class="info-group"><strong>Alergia:</strong> {{paciente.alergiaMedicamento}}</p>
      <p class="info-group"><strong>Medicação de controle:</strong> {{paciente.tipoMedicamento}}</p>
      <p class="info-group"><strong>Plano de saúde:</strong> {{paciente.planoSaude}}</p>
      <p class="info-group"><strong>Cartão SUS:</strong> {{paciente.cartaoSus}}</p>
      <p class="info-group"><strong>Seguro de vida:</strong> {{paciente.seguroVida}}</p>
      <p class="info-group"><strong>Deficiência:</strong> {{paciente.deficiencia}}</p>
    </div>

    <div class="section">
      <h3>Filhos</h3>
      {{#if paciente.filhos.length}}
        <ul>
          {{#each paciente.filhos}}
            <li>{{this.nome}} — {{this.idade}} anos</li>
          {{/each}}
        </ul>
      {{else}}
        <p>Sem filhos cadastrados.</p>
      {{/if}}
    </div>

    <div class="section">
      <h3>Observações</h3>
      <p>{{paciente.observacoes}}</p>
    </div>

    <a href="/pacientes" class="btn-primary">Voltar</a>
  </div>
</body>
</html>