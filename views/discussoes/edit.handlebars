<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Discussão de Caso</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h2>Editar Discussão de Caso</h2>
        <form action="/discussoes/{{discussaoCaso.id}}" method="POST">
            <input type="hidden" name="_method" value="PUT">

            <div class="form-group">
                <label for="conteudo">Conteúdo:</label>
                <textarea name="conteudo" id="conteudo" required>{{discussaoCaso.conteudo}}</textarea>
            </div>

            <div class="form-group">
                <label for="atendimentoId">Atendimento:</label>
                <select name="atendimentoId" id="atendimentoId" required>
                    {{#each atendimentos}}
                        <option value="{{this.id}}" {{#if (eq this.id discussaoCaso.atendimentoId)}}selected{{/if}}>
                            {{this.nomePaciente}} (ID: {{this.id}})
                        </option>
                    {{/each}}
                </select>
            </div>

            <button type="submit" class="btn btn-success">Atualizar</button>
            <a href="/discussoes/{{discussaoCaso.id}}" class="btn btn-secondary">Cancelar</a>
        </form>
    </div>
</body>
</html>