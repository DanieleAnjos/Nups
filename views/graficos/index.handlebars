<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Estatísticas</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
    }
    h1, h2 {
      color: #333;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }
    th, td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: left;
    }
    th {
      background-color: #f2f2f2;
    }
  </style>
</head>
<body>
  <h1>Estatísticas de Pacientes, Encaminhamentos e Atendimentos</h1>

  <h2>Pacientes por Período</h2>
  <table>
    <thead>
      <tr>
        <th>Mês/Ano</th>
        <th>Quantidade de Pacientes</th>
      </tr>
    </thead>
<tbody>
  {{#each pacientesLabels}}
    <tr>
      <td>{{this}}</td>
      <td>{{lookup ../pacientesData @index}}</td>
    </tr>
  {{/each}}
</tbody>

  </table>

  <h2>Encaminhamentos Realizados por Período</h2>
  <table>
    <thead>
      <tr>
        <th>Mês/Ano</th>
        <th>Quantidade de Encaminhamentos</th>
      </tr>
    </thead>
    <tbody>
      {{#each encaminhamentosLabels}}
        <tr>
          <td>{{this}}</td>
          <td>{{lookup ../encaminhamentosData @index}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <h2>Atendimentos por Período</h2>
  <table>
    <thead>
      <tr>
        <th>Mês/Ano</th>
        <th>Quantidade de Atendimentos</th>
      </tr>
    </thead>
    <tbody>
      {{#each atendimentosLabels}}
        <tr>
          <td>{{this}}</td>
          <td>{{lookup ../atendimentosData @index}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</body>
</html>
