<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alterar Senha</title>
    <link rel="stylesheet" href="/CSS/Styles_Login.css">
</head>
<body>
    <div class="login-container">
        <!-- Logo da Empresa -->
        
        <h1>Alterar Senha</h1>

        <!-- Exibe mensagens de erro ou sucesso -->
        {{#if error_msg}}
            <div class="message error" aria-live="polite">{{error_msg}}</div>
        {{/if}}
        {{#if success_msg}}
            <div class="message success" aria-live="polite">{{success_msg}}</div>
        {{/if}}


    {{#if error}}
        <p class="alert">{{error}}</p>
    {{/if}}
    
    {{#if success}}
        <p class="success">{{success}}</p>
    {{/if}}

        <!-- Formulário para alterar a senha -->
        <form action="/auth/changePassword" method="POST">
            <div>
                <input type="text" name="usuario" placeholder="Usuário" autocomplete="username" required autofocus>
            </div>

            <div>
                <input type="password" name="senhaAtual" placeholder="Senha Atual" autocomplete="current-password" required>
            </div>

            <div>
                <input type="password" name="novaSenha" placeholder="Nova Senha" autocomplete="new-password" required>
            </div>

            <button type="submit">Alterar Senha</button>
        </form>

        <!-- Link para voltar para a página de login -->
        <button class="btn btn-secondary btn-sm" onclick="history.back()">
            <i class="fa fa-arrow-left"></i> Voltar
        </button>
    </div>
</body>
</html>

<style>
/* Manter a mesma estrutura de estilo da página de Login */
body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
}

.login-container {
    background-color: white;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
    width: 300px;
    text-align: center;
}

.logo {
    max-width: 150px;
    margin-bottom: 20px;
}

h1 {
    font-size: 1.5rem;
    margin-bottom: 20px;
}

input {
    width: 100%;
    padding: 10px;
    margin: 10px 0;
    border: 1px solid #ccc;
    border-radius: 5px;
}

button {
    width: 100%;
    background: #28a745;
    color: white;
    border: none;
    padding: 10px;
    cursor: pointer;
    border-radius: 5px;
}

button:hover {
    background: #218838;
}

.forgot-password {
    margin-top: 15px;
    text-align: center;
}

.forgot-link {
    font-size: 0.9rem;
    color: #007BFF;
    text-decoration: none;
}

.forgot-link:hover {
    text-decoration: underline;
    color: #0056b3;
}

.message {
    font-size: 0.9rem;
    margin-bottom: 10px;
}

.message.error {
    color: red;
}

.message.success {
    color: green;
}
        .alert {
            color: red;
            font-size: 14px;
            text-align: center;
        }
        .success {
            color: green;
            font-size: 14px;
            text-align: center;
        } 
</style>
