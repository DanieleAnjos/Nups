<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="card-title text-center text-primary mb-4">Editar Sala</h2>

                    <form action="/salas/{{ sala.id }}" method="POST">
                        <!-- Método para edição -->
                        <input type="hidden" name="_method" value="PUT">

                        <!-- Campo Nome -->
                        <div class="mb-3">
                            <label for="nome" class="form-label">Nome:</label>
                            <input type="text" id="nome" name="nome" required class="form-control" minlength="3" maxlength="100" placeholder="Digite o nome da sala" value="{{ sala.nome }}">
                        </div>

                        <!-- Campo Capacidade -->
                        <div class="mb-3">
                            <label for="capacidade" class="form-label">Capacidade:</label>
                            <input type="number" id="capacidade" name="capacidade" required class="form-control" min="1" placeholder="Número máximo de pessoas" value="{{ sala.capacidade }}">
                        </div>

                        <!-- Campo Descrição -->
                        <div class="mb-3">
                            <label for="descricao" class="form-label">Descrição:</label>
                            <input type="text" id="descricao" name="descricao" maxlength="255" class="form-control" placeholder="Digite uma breve descrição da sala" value="{{ sala.descricao }}">
                        </div>

                        <div class="d-flex justify-content-end gap-3">
                            <a href="/salas" class="btn btn-outline-secondary px-4">Voltar</a>
                            <button type="submit" class="btn btn-primary px-4">Salvar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Incluir o script do Bootstrap -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

<style>
    .btn {
        margin-top: 1rem;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .card {
        border: none;
        border-radius: 0.5rem;
    }

    .card-body {
        padding: 30px;
    }

    .form-label {
        font-weight: bold;
    }

    .form-control {
        border-radius: 0.5rem;
        padding: 10px;
    }

    /* Estilo para o botão de salvar */
    .btn-primary {
        background-color: #007bff;
        border-color: #007bff;
    }

    /* Estilo para o botão de voltar */
    .btn-outline-secondary {
        color: #6c757d;
        border-color: #6c757d;
    }

    /* Efeito de hover para os botões */
    .btn-primary:hover {
        background-color: #0056b3;
        border-color: #0056b3;
    }

    .btn-outline-secondary:hover {
        background-color: #e2e6ea;
        border-color: #6c757d;
    }
</style>